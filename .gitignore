build/
build-*/
out/
.cache/
compile_commands.json
CMakeUserPresets.json
*.user
.vs/
.vscode/
.idea/
*.o
*.obj
*.exe
*.dll
*.a
*.so
.claude/

# What the dock probes write where they are run from. Run artifacts, and the
# whole point of them is that each run replaces the last.
dock_probe.log
window_probe.log

# Editor and tool caches
.dirac-cache/

# Packaging scratch: everything here is rebuilt by .github/workflows/ci.yml
dist/
stage/
AppDir/
*.AppImage
