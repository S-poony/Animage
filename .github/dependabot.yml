version: 2
updates:
  # The workflow pins actions by major version; this bumps those pins when a
  # new major appears, so CI does not quietly rot.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
